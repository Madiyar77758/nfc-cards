/* ============================================================
   Единственный файл, который правят руками при переезде воркера.
   Меню читается из него; если воркер недоступен, страницы сами
   откатываются на data/menu.json из репозитория и показывают
   последние сохранённые цены.
   Воркер развёрнут 26 августа 2026, см. api/README.md.
   ============================================================ */
window.XD_CONFIG = {
  API_BASE: 'https://xd-menu.madiyar.workers.dev',
  SLUG: 'xamidoo'          // код заведения внутри воркера
};
