/* ============================================================
   Единственный файл, который правят руками после деплоя воркера.
   Пока API_BASE пустой — сайт живёт на data/menu.json из репозитория,
   а админка отдаёт готовый файл на скачивание.
   После `npx wrangler deploy` (см. api/README.md) впишите сюда адрес,
   который напечатает wrangler, и админка начнёт сохранять по-настоящему.
   ============================================================ */
window.XD_CONFIG = {
  API_BASE: '',            // например 'https://xd-menu.ваш-логин.workers.dev'
  SLUG: 'xamidoo'          // код заведения внутри воркера
};
