name = "xd-menu"
main = "worker.js"
compatibility_date = "2026-08-01"

# database_id подставит команда `npx wrangler d1 create xd-menu` —
# скопируйте её вывод сюда вместо строки ниже.
[[d1_databases]]
binding = "DB"
database_name = "xd-menu"
database_id = "ВСТАВЬТЕ_ID_ИЗ_wrangler_d1_create"
